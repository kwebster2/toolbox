  <%= form_tag(cohort_tags_path(cohort.slug), method: "get", class: "navbar-form navbar-left", role: "search") do %>
  <div class="form-group">
    <%= text_field_tag :search, params[:search], placeholder: "Search by tags", id: "tag-autocomplete", class: "controls" %>
  </div>
    <%= submit_tag "Search", class: "btn btn-default" %>
  <% end %>
