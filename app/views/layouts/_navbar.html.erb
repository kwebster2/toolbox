<nav class="navbar navbar-default">
  <div class="container-fluid">

    <ul class="nav navbar-nav">
      <li><a href="<%= root_path %>">Home</a></li>
    </ul>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Cohorts<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="<%= cohort_path("web-development")%>">Web</a></li>
            <li><a href="<%= cohort_path("ios")%>">iOS</a></li>
          </ul>
        </li>

        <li><a href="<%= user_path(current_user.user_name) %>">Bookmarks</a></li>

      </ul>

      <% if @cohort %>
        <%= render partial: 'layouts/tag_form', locals: { cohort: @cohort } %>
      <% end %>


      <ul class="nav navbar-nav navbar-right">
        <% if session[:user_id] %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><img src="<%= current_user.avatar_url %>" class="navbar-avatar"><%= current_user.user_name %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="<%= user_path(current_user.user_name) %>">Profile</a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign Out", logout_path, method: :post %></li>
          </ul>
        </li>
        <% else %>
        <%= button_to "Sign In", 'auth/github', class: "btn btn-default navbar-btn"%>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
